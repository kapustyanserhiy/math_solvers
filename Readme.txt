Math Function — Test

Overview
This small test project validates basic GitHub workflows (commit, push, CI) and demonstrates a simple math function implementation with unit tests. It is intended as a lightweight repository to exercise development, testing, and continuous-integration practices.

What's included
- Source code for the math function(s) (directory: math_solvers/)
- Unit tests (directory: tests/)
- CI configuration (.github/workflows/) to run tests automatically

Quick start
1. Clone the repository:
    git clone <repo-url>

2. (Optional) Create and activate a virtual environment:
    python -m venv .venv
    source .venv/bin/activate   # macOS / Linux
    .venv\Scripts\activate      # Windows

3. Install dependencies (if any):
    pip install -r requirements.txt

4. Run tests:
    pytest


License
See the LICENSE file for licensing details.
